const CustomeNextArrow = ({ onClick }) => {
  return (
    <button className='icon-btn custom-arrow next-arrow' onClick={onClick}>
      <span className='material-symbols-outlined'>arrow_forward</span>
    </button>
  );
};
export default CustomeNextArrow;
